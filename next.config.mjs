import path from "path";
import { fileURLToPath } from "url";

const __dirname = path.dirname(fileURLToPath(import.meta.url));

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Static HTML export → creates /out folder on `next build` / `npm run export`
  output: "export",
  // Fix wrong workspace root when parent folders also have package-lock.json
  outputFileTracingRoot: __dirname,
  transpilePackages: ["@google/model-viewer"],
  images: {
    // Required for static export (Image Optimization API is not available)
    unoptimized: true,
    formats: ["image/avif", "image/webp"],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 3840],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
    minimumCacheTTL: 60,
    dangerouslyAllowSVG: true,
    contentSecurityPolicy: "default-src 'self'; script-src 'none'; sandbox;",
  },
};

export default nextConfig;
